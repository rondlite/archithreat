/* PLACEHOLDER — vendor real htmx 1.9.x bundle here before release. */
/* TODO: replace this file with the real htmx.min.js during the build/release
 * pipeline (see docs/self-hosting.md). The web shell ships an HTMX-driven UI
 * but does not vendor third-party JavaScript in source control; CI copies the
 * verified vendor bundle into this path.
 */
