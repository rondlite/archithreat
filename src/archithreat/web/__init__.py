"""archithreat web shell: FastAPI JSON API + HTMX UI.

This package is loaded only when the ``[web]`` extra is installed. The shell is
a thin wrapper around the conversion core; it adds no business logic and never
writes user content to disk.
"""
